# finding-letter-e-in-a-user-defined-string
#program to check if e is present in a given string
s= input('enter a string')
for i in s:
	if 'e' == i :
		print('e is present in ',s,s.count("e"),'times'
		if s.count('e') != 0 :
			break
	
